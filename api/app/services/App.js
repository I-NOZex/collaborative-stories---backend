'use strict';

// Public dependencies.
const _ = require('lodash');

module.exports = {
  initDatabase: async () => {
    /*const data = await Wordlist.find();

    if (_.isEmpty(data)) {
      _.forEach(strapi.api.app.config.data, async word => {
        await strapi.api.wordlist.services.wordlist.add(word);
      });
    }*/
  }
};
